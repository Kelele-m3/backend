{
  "name": "backend-root",
  "private": true,
  "description": "Root workspace; all backend source and config live in ./backend",
  "scripts": {
    "build": "npm run build --prefix backend",
    "start": "npm run start --prefix backend",
    "start:dev": "npm run start:dev --prefix backend",
    "start:prod": "npm run start:prod --prefix backend",
    "lint": "npm run lint --prefix backend",
    "test": "npm run test --prefix backend",
    "test:e2e": "npm run test:e2e --prefix backend",
    "create:db": "npm run create:db --prefix backend"
  }
}
